# Infrastructure

DynamoDB table and other resources are defined in the root [template.yaml](../template.yaml).

This folder can hold additional infrastructure code (e.g. CDK constructs) if needed later.
