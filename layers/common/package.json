{
  "name": "common",
  "version": "1.0.0",
  "description": "Shared utilities and services for Lambda functions",
  "main": "index.js",
  "private": true,
  "scripts": {
    "build": "tsc && cp package.json nodejs/node_modules/common/ && cd nodejs/node_modules/common && npm install --production"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/client-s3": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0",
    "busboy": "^1.6.0",
    "csv-parse": "^6.1.0"
  },
  "devDependencies": {
    "@types/busboy": "^1.5.4",
    "@types/node": "^25.2.2",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
